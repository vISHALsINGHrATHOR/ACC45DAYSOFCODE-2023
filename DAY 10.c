#include <stdio.h>
int main() {
   int a,b,c;
   // insertingthe amount Chef has and the cost of one chocolate respectively.
   scanf("%d %d",&a,&b);
   c=a/b
   // printing the maximum number of chocolates Chef can buy
   printf("%d",c);
}